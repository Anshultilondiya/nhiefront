import React from 'react';

const Card = (props) => {
    return (
        <div>
            <h1>Never I Have Ever</h1>
            <h1>{props.ques}</h1>
        </div>
    );
};

export default Card;
